function handleSend(ev:any){
    ev.preventDefault();
    const inputelem = ev.target.elements;
    let result = {};

    
    // for (let i = 0; i < inputelem.length; i++)
    // {
    //     if (inputelem[i].name === 'username'){

    //     }
    // }
}

function handleCheck(event){

}
