// // import { map } from "jquery"

// const arr1 = [1,4,3]
// const arr2 = [2,5,1]
// const arr3 = [] 
// // const array= arr1.map(i=> )

// for (let i=0; i<arr3.length, i++){
//     arr3.push (arr1[y]+arr2[y])
// }
// console.log(arr3)

// /******** */

// const sum = arr1.map ((i,index) => i+arr2[index])

// console.log (sum)

const num1=+prompt (`enter 1st number which is *only* under 999`);
const num2= +prompt (`enter 2nd number which is *only* under 999`);


if( num1 % 3 == 0){
    console.log (`${num1} is divisible by 3`)
}
    else{
        alert (`not divisable by 3`)
    }

    if( num2 % 3 == 0){
        console.log (`${num2} is divisible by 3`)
    }
        else{
            alert (`not divisable by 3`)
        }
    
